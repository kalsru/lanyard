[build]
builder = "nixpacks"
buildCommand = "npx playwright install chromium --with-deps && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[healthcheck]
httpPath = "/api/health"
timeoutSeconds = 30
